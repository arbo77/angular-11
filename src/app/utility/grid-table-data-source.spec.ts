import { GridTableDataSource } from './grid-table-data-source';

describe('GridTableDataSource', () => {
  it('should create an instance', () => {
    expect(new GridTableDataSource()).toBeTruthy();
  });
});
